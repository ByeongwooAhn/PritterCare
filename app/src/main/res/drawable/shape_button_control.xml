<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- 내부 색상 -->

    <!-- 테두리 색상과 두께 설정 -->
    <stroke
        android:width="1dp"
        android:color="@color/controlButtonBorderColor" />

    <!-- 모서리 둥글기 설정 -->
    <corners
        android:radius="15dp"/>
</shape>
