<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/rippleColor01"> <!-- ripple 색상 설정 -->

    <!-- 기본 배경 모양 -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/basicColor02" />
            <corners android:radius="@dimen/main_btn_border_radius"/>
        </shape>
    </item>
</ripple>